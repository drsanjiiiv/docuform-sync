# Node / dependencies
node_modules/

# clasp config & auth (script ID + credentials) - keep local
.clasp.json
.clasprc.json

# OAuth client secrets - never commit
client_secret_*.json

# AI coding session data
.continue/

# Internal verification working notes
*.txt

# Session/local docs (kept on disk, not tracked in git; already excluded from clasp via *.md)
CODEBASE.md
SESSION_NOTES.md
FEATURES.md
SCOPES.md
